spring:
  cloud:
    config:
      name: eureka-client
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: eureka-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/